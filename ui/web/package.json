{
  "name": "@dante/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DANTE web UI — Vite + TypeScript + Three.js procedural brain and chat",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "@types/three": "^0.166.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
